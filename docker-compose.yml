version: '3.8'

services:
  proxmox-mcp:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    container_name: proxmox-mcp
    restart: unless-stopped

    environment:
      - PROXMOX_NODES=${PROXMOX_NODES}
      - PROXMOX_PORT=${PROXMOX_PORT:-8006}
      - PROXMOX_TOKEN_ID=${PROXMOX_TOKEN_ID}
      - PROXMOX_TOKEN_SECRET=${PROXMOX_TOKEN_SECRET}
      - PROXMOX_NODE_TOKENS=${PROXMOX_NODE_TOKENS}
      - JWT_SECRET=${JWT_SECRET}
      - ADMIN_PASSWORD=${ADMIN_PASSWORD}
      - OPERATOR_PASSWORD=${OPERATOR_PASSWORD}
      - VIEWER_PASSWORD=${VIEWER_PASSWORD}
      - VERIFY_TLS=${VERIFY_TLS:-false}
      - LOG_LEVEL=${LOG_LEVEL:-INFO}
      - ALLOWED_OPERATIONS=${ALLOWED_OPERATIONS:-vm.list,vm.status,vm.start,vm.stop,vm.shutdown,vm.create,vm.clone,vm.snapshot,node.list,node.status,storage.list,backup.list,backup.status}

    ports:
      - "8000:8000"

    networks:
      - mcp-network

    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=64m

    security_opt:
      - no-new-privileges:true

    cap_drop:
      - ALL

    user: 1000:1000

    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/health').status_code == 200"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s

    labels:
      - mcp.service=proxmox
      - mcp.version=${MCP_VERSION:-1.0.0}

networks:
  mcp-network:
    driver: bridge
    ipam:
      config:
        - subnet: 172.28.0.0/16
